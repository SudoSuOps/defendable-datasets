# Example Data

This folder is reserved for small, reviewable sample rows that demonstrate dataset shape.

The v0 registry entries are demo metadata unless their file hashes point to real committed files or durable object storage.
